const changeH1 = text => document.querySelectorAll('h1').forEach(h => h.innerHTML = text)

const changeP = par => document.querySelectorAll('p').forEach(p => p.innerHTML = par)

const changeMenu = menu => document.querySelectorAll('li').forEach(l => l.innerHTML = menu)


